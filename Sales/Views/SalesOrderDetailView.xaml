<UserControl x:Class="Sales.Views.SalesOrderDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Margin="24"
            CornerRadius="8"
            Background="{DynamicResource MaterialDesignPaper}"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="1"
            Effect="{DynamicResource MaterialDesignShadowEffect}">

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <materialDesign:PackIcon Kind="ClipboardText"
                                         Width="32"
                                         Height="32"
                                         Foreground="{DynamicResource PrimaryHueMidBrush}"
                                         Margin="0,0,12,0"/>

                <TextBlock Text="{Binding Title}"
                           FontSize="22"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                           VerticalAlignment="Center"/>
            </StackPanel>


            <Grid Grid.Row="1" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

            
                <TextBlock Grid.Row="0" Grid.Column="0"
                           Text="Customer"
                           VerticalAlignment="Center"
                           FontSize="14"
                           Foreground="{DynamicResource MaterialDesignBody}"/>

                <ComboBox Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding Customers}"
                          SelectedItem="{Binding SelectedCustomer}"
                          DisplayMemberPath="Name"
                          materialDesign:HintAssist.Hint="Select customer"
                          FontSize="14"
                          Margin="0,0,0,8"
                          IsEnabled="{Binding IsCustomerSelectionEnabled}"/>

   
                <TextBlock Grid.Row="1" Grid.Column="0"
                           Text="Add Product"
                           VerticalAlignment="Center"
                           FontSize="14"
                           Foreground="{DynamicResource MaterialDesignBody}"/>

                <Grid Grid.Row="1" Grid.Column="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0"
                              ItemsSource="{Binding Products}"
                              SelectedItem="{Binding SelectedProduct}"
                              DisplayMemberPath="Name"
                              materialDesign:HintAssist.Hint="Select product"
                              FontSize="14"
                              Margin="0,0,0,8"/>
                    <TextBox Grid.Column="1"
                             Text="{Binding ProductQuantity, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Quantity"
                             Width="80"
                             Margin="8,0,0,0"/>
                    <Button Grid.Column="2"
                            Content="Add"
                            Command="{Binding AddLineCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="{DynamicResource PrimaryHueMidBrush}"
                            Foreground="Black"
                            Margin="8,0,0,0"/>
                </Grid>
            </Grid>

            <DataGrid Grid.Row="2"
                      ItemsSource="{Binding TemporaryOrderLines}"
                      AutoGenerateColumns="False"
                      Margin="0,16,0,16"
                      HeadersVisibility="Column"
                      BorderThickness="0"
                      
                      Background="{DynamicResource MaterialDesignPaper}">
                <DataGrid.Resources>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Height" Value="30"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn IsReadOnly="True"  Header="Product" Binding="{Binding Product.Description}" Width="*"/>
                    <DataGridTextColumn IsReadOnly="True" Header="Quantity" Binding="{Binding Quantity}" Width="Auto"/>
                    <DataGridTextColumn IsReadOnly="True" Header="Unit Price" Binding="{Binding UnitPrice, StringFormat=C}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>

    
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <TextBlock Text="Total:" FontWeight="Bold" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding TotalAmount, Mode=OneWay}" />
            </StackPanel>

            <StackPanel Grid.Row="4"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Content="CANCEL"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Foreground="{DynamicResource PrimaryHueMidBrush}"
                        Width="120"
                        Height="40"
                        FontSize="14"
                        Margin="0,0,12,0">
                    <materialDesign:ButtonAssist.CornerRadius>4</materialDesign:ButtonAssist.CornerRadius>
                </Button>

                <Button Content="CONFIRM ORDER"
                        Command="{Binding ConfirmCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        Foreground="Black"
                        Width="160"
                        Height="40"
                        FontSize="14">
                    <materialDesign:ButtonAssist.CornerRadius>4</materialDesign:ButtonAssist.CornerRadius>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>